---
import { SITE_TITLE, NAV_LINKS as links } from "@/consts";
import ThemeToggle from "./ThemeToggle.astro";
---

<nav class="w-full !z-[9999] py-5 px-4 bg-white/90 dark:bg-black/90 backdrop-blur-sm border-b border-zinc-200 dark:border-zinc-800 transition-colors">
  <div
    class="w-full grid gap-4 md:px-5 place-items-center grid-cols-1 md:flex md:justify-between md:px-4"
  >
    <a href="/">
      <div
        id="logo"
        class="pt-2 pb-1 !grid !place-items-center select-none leading-none border-2 border-black dark:border-white hover:bg-black dark:hover:bg-white bg-white dark:bg-black scale-110 hover:text-white dark:hover:text-black text-black dark:text-white px-4 transition-all ease-in-out"
      >
        {SITE_TITLE}
      </div>
    </a>
    <div class="flex flex-wrap gap-4 md:pr-5">
      <div class="hidden md:block">/</div>
      {
        links.map((link) => {
          const href =
            link.href ?? link.title.toLowerCase().replaceAll(" ", "-");
          const pathname = Astro.url.pathname.replace(
            import.meta.env.BASE_URL,
            "",
          );
          const subpath = pathname.match(/[^\/]+/g);
          const isActive =
            href === pathname || href === "/" + (subpath?.[0] || "");
          return (
            <>
              <a
                class:list={[{ "bg-white text-black": isActive }]}
                class="hover:bg-zinc-200 dark:hover:bg-white dark:hover:text-black transition-all ease-in-out px-2 max-w-fit text-zinc-600 dark:text-zinc-300"
                href={`/${href}`}
              >
                <div>{link.title}</div>
              </a>
              <div class="hidden md:block">/</div>
            </>
          );
        })
      }
      <ThemeToggle />
    </div>
  </div>
</nav>

<script>
  window.addEventListener("scroll", () => {
    const logo = document.getElementById("logo");
    if (!logo) return;
    if (window.scrollY > 100) {
      logo.className =
        "pt-2 pb-1 !grid !place-items-center select-none leading-none border-2 border-black dark:border-white hover:bg-white dark:hover:bg-black bg-black dark:bg-white scale-100 hover:text-black dark:hover:text-white text-white dark:text-black px-4 transition-all ease-in-out";
    } else {
      logo.className =
        "pt-2 pb-1 !grid !place-items-center select-none leading-none border-2 border-black dark:border-white hover:bg-black dark:hover:bg-white bg-white dark:bg-black scale-110 hover:text-white dark:hover:text-black text-black dark:text-white px-4 transition-all ease-in-out";
    }
  });
</script>
